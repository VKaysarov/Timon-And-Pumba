const AppearanceComponent = (
    img = new Image(),
    frames = 0,
    currentFrame = 0,
    speedAnim = 0,
    sprite = 0,
) => ({
    img,
    frames,
    currentFrame,
    speedAnim,
    sprite,
})